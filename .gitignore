node_modules/
dist/
data/
repos/
logs/
.env
.encryption-key
*.db
*.db-journal
*.db-wal
reports/
.stryker-tmp/
stryker-incremental.json

# Internal plans — not for public repo
docs/plans/

# Build artifacts
*.tsbuildinfo

# Built web bundle — regenerated by `npm run build` and by the Docker image
# build. Committing it churns the working tree with new hash-suffixed
# filenames every time a contributor builds locally.
packages/api/public/
